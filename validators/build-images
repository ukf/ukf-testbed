#!/bin/bash

docker buildx build \
    --file Dockerfile.010 \
    --tag ukf/testbed:0.10 \
    --output type=docker \
    $@ \
    .

docker buildx build \
    --file Dockerfile.010x \
    --tag ukf/testbed:0.10-xalan \
    --output type=docker \
    $@ \
    .
